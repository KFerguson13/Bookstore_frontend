import BookList from "../components/bookList"

/** This component just returns what the booklist returns. It's existence is to make the application slightly more organized. */
const Products = () => {
    return <BookList />
}

export default Products;